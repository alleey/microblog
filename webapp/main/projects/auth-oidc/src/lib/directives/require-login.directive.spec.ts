import { RequireLoginDirective } from './require-login.directive';

describe('RequireLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireLoginDirective();
    expect(directive).toBeTruthy();
  });
});
