import { RequireOwnerDirective } from './require-owner.directive';

describe('RequireOwnerDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireOwnerDirective();
    expect(directive).toBeTruthy();
  });
});
